// Tailwind config moved to external file (kept after the CDN script as in original)
window.tailwind = window.tailwind || {};
tailwind.config = {
    theme: {
        extend: {
            colors: {
                primary: '#4f46e5',
                secondary: '#64748b',
            }
        }
    }
};
